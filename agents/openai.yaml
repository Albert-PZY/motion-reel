interface:
  display_name: "Motion Reel"
  short_description: "Create Remotion videos with Codex"
  default_prompt: "Use $motion-reel to build a narrated Remotion explainer video with scene-timed audio."
policy:
  allow_implicit_invocation: true

